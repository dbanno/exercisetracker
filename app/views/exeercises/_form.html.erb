<%= form_for(@exeercise) do |f| %>
  <% if @exeercise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@exeercise.errors.count, "error") %> prohibited this exeercise from being saved:</h2>

      <ul>
      <% @exeercise.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
